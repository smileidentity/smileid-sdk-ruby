# frozen_string_literal: true

source 'https://rubygems.org'

# Puts this example's own lib/ on the load path, so `require 'smileid_example'`
# works without RUBYOPT tricks.
gemspec

gem 'usesmileid', path: '..'

group :development, :test do
  gem 'rspec', '~> 3.13'
  gem 'rubocop', '~> 1.68'
end
